{
  "name": "restaurant-app",
  "version": "1.0.0",
  "description": "",
  "main": "y",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^5.3.0",
    "config": "^3.3.3",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.11.13",
    "path": "^0.12.7"
  },

  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ./dlient/mern-stack && npm run build",
    "install-client": "cd ./client/mern-stack && npm run build",
    "heroku-postbuild": "npm run install-client && npm run build", 
    "start": "node server.js",
    "server": "nodemon server.js"
  },
  "author": "ikzath",
  "license": "ISC",
      "engines": {
      "node": "12.18.3"
    }
}
